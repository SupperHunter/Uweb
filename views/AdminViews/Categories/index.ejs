<h1 class="text-center my-4 text-primary fw-bold">Tất Cả Danh Mục Sản Phẩm</h1>

<div class="container">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <a href="/admin/createCategory" class="btn btn-success">
      ➕ Thêm Mới Danh Mục
    </a>
  </div>

  <div class="table-responsive shadow-sm rounded">
    <table class="table table-bordered table-hover align-middle">
      <thead class="table-dark">
        <tr class="text-center">
          <th>Tên</th>
          <th>Mô Tả</th>
          <th>Hình Ảnh</th>
          <th>Hành Động</th>
        </tr>
      </thead>
      <tbody>
        <% categories.forEach(category=> { %>
          <tr>
            <td><strong>
                <%= category.name %>
              </strong></td>
            <td>
              <%- category.Description.length> 50 ? category.Description.substring(0, 50) + '...' : category.Description
                %>
            </td>
            <td class="text-center">
              <% if (category.imageUrl.startsWith('http')) { %>
                <img src="<%= category.imageUrl %>" alt="product image" class="rounded shadow-sm"
                  style="width: 60px; height: 60px; object-fit: cover;">
                <% } else { %>
                  <img src="data:image/jpeg;base64,<%= category.imageUrl %>" alt="product image"
                    class="rounded shadow-sm" style="width: 60px; height: 60px; object-fit: cover;">
                  <% } %>
            </td>
            <td class="text-center">
              <a href="/admin/editcategory/<%= category.id %>" class="btn btn-warning btn-sm me-1">✏️ Sửa</a>
              <button class="btn btn-danger btn-sm" data-bs-toggle="modal"
                data-bs-target="#deleteModal<%= category.id %>">🗑️ Xóa</button>

              <!-- Modal xác nhận xóa -->
              <div class="modal fade" id="deleteModal<%= category.id %>" tabindex="-1"
                aria-labelledby="deleteModalLabel" aria-hidden="true">
                <div class="modal-dialog modal-dialog-centered">
                  <div class="modal-content">
                    <div class="modal-header bg-danger text-white">
                      <h5 class="modal-title" id="deleteModalLabel">❗ Xác nhận xóa</h5>
                      <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"
                        aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                      Bạn có muốn xóa danh mục "<strong>
                        <%= category.name %>
                      </strong>"?
                    </div>
                    <div class="modal-footer">
                      <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                      <form action="/admin/categories/<%= category.id %>/delete" method="POST" class="d-inline">
                        <input type="hidden" name="authToken" id="authToken">
                        <button type="submit" class="btn btn-danger">Đồng ý xóa</button>
                      </form>
                    </div>
                  </div>
                </div>
              </div>
            </td>
          </tr>
          <% }) %>
      </tbody>
    </table>
  </div>
</div>

<!-- Token -->
<script>
  const token = localStorage.getItem('authToken');
  if (token) {
    document.querySelectorAll('input[name="authToken"]').forEach(input => {
      input.value = token;
    });
  }
</script>

<!-- Bootstrap -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>